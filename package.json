{
  "name": "threadlight",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build --workspace @threadlight/desktop",
    "build:packages": "tsc -b",
    "clean": "tsc -b --clean",
    "desktop:dev": "npm run build:packages && npm run dev --workspace @threadlight/desktop",
    "desktop:preview": "npm run build:packages && npm run preview --workspace @threadlight/desktop",
    "test": "npm run build && vitest run",
    "typecheck": "tsc -b --pretty false && npm run typecheck --workspace @threadlight/desktop",
    "start": "node packages/app-server/dist/bin.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
