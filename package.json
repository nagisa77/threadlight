{
  "name": "threadlight",
  "version": "0.1.0",
  "description": "An observable, recoverable, provider-neutral agent runtime and desktop client.",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nagisa77/threadlight.git"
  },
  "bugs": {
    "url": "https://github.com/nagisa77/threadlight/issues"
  },
  "homepage": "https://github.com/nagisa77/threadlight#readme",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build --workspace @threadlight/desktop && npm run build --workspace @threadlight/web",
    "build:packages": "tsc -b && node scripts/copy-runtime-assets.mjs",
    "clean": "tsc -b --clean",
    "desktop:dev": "npm run build:packages && npm run dev --workspace @threadlight/desktop",
    "desktop:package": "npm run build:packages && npm run package --workspace @threadlight/desktop",
    "desktop:preview": "npm run build:packages && npm run preview --workspace @threadlight/desktop",
    "web:dev": "npm run build:packages && npm run dev --workspace @threadlight/web --",
    "web:build": "npm run build:packages && npm run build --workspace @threadlight/web",
    "web:preview": "npm run web:build && npm run preview --workspace @threadlight/web",
    "test": "npm run build && vitest run",
    "typecheck": "tsc -b --pretty false && npm run typecheck --workspace @threadlight/desktop && npm run typecheck --workspace @threadlight/web",
    "start": "node packages/app-server/dist/bin.js",
    "host": "node packages/app-server/dist/host-bin.js",
    "host:dev": "npm run build:packages && npm run host --",
    "host:package": "npm run build:packages && node scripts/package-host.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
